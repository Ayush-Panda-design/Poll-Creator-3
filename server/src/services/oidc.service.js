/**
 * OIDC Service (stub)
 * ───────────────────
 * Extend this file to add full OpenID Connect token verification,
 * user info endpoint calls, and JWT claim extraction.
 *
 * For now, auth is handled in auth.service.js → googleAuthService()
 * which accepts verified profile data sent from the frontend.
 */

export {};
